# Core dependencies
agno>=2.4.7
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# Optional but recommended for your setup
openai>=1.3.0  # Required for agno.models.openai
langchain>=0.0.350  # For knowledge base enhancements
chromadb>=0.4.22  # If using vector database
sentence-transformers>=2.2.2  # For embeddings